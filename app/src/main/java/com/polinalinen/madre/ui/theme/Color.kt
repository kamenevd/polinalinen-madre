package com.polinalinen.madre.ui.theme

import androidx.compose.ui.graphics.Color

// ═══ Polina's aesthetic: warm sourdough bakery tones ═══
// Inspired by fresh bread, mustard seeds, honey, linen

// ═══ Light Theme — Warm Bread / Mustard ═══
val BackgroundDark = Color(0xFFF2E4CC)          // Warm bread/mustard — main background
val BackgroundCard = Color(0xFFFFF3E0)           // Light cream — card background
val BackgroundCardHover = Color(0xFFFFF8F0)      // Almost white warm — hover/pressed

// ═══ Text ═══
val TextPrimary = Color(0xFF3A2A1A)              // Dark brown — like roasted crust
val TextSecondary = Color(0xFF7A6A58)            // Warm muted brown
val TextAccent = Color(0xFF8B5E2B)               // Deep gold-brown — accent text

// ═══ Accents — bakery tones ═══
val AccentGold = Color(0xFFB8862D)               // Deep mustard gold (primary accent)
val AccentRose = Color(0xFFB85C4A)               // Warm terracotta — tomato/berry
val AccentCream = Color(0xFFF5E6C8)              // Cream — bread crumb
val AccentBrown = Color(0xFF6B4F30)              // Warm brown — baked bread edge

// ═══ Status ═══
val StatusAction = Color(0xFFB8862D)             // Gold for "action" steps
val StatusWait = Color(0xFF5B8A8F)               // Soft teal for "wait" steps
val StatusActive = Color(0xFF8B5E2B)             // Deep gold active indicator
val StatusCompleted = Color(0xFF6B8F5B)          // Natural green for done

// ═══ Timers ═══
val TimerBackground = Color(0xFFE8D5B8)          // Timer warm bg
val TimerProgress = Color(0xFFB8862D)            // Gold progress
val TimerUrgent = Color(0xFFB85C4A)              // Terracotta when urgent

// ═══ Dividers ═══
val DividerColor = Color(0xFFD9C9B0)             // Warm subtle divider

// ═══ Extra — from Polina's palette & bread ═══
val LinenColor = Color(0xFFE8E0D4)               // Linen cloth
val CrustDark = Color(0xFF6B4F30)                // Dark crust
val CrumbPale = Color(0xFFF5E6C8)                // Pale bread crumb
val PolinaNavy = Color(0xFF1B365D)               // Deep navy (from site)

// ═══ Difficulty colors (from app palette) ═══
val DifficultyEasy = Color(0xFF6B8F5B)           // Green (StatusCompleted)
val DifficultyMedium = Color(0xFFB8862D)         // Gold (AccentGold)
val DifficultyHard = Color(0xFFB85C4A)           // Terracotta (AccentRose)
