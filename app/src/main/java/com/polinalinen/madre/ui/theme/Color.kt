package com.polinalinen.madre.ui.theme

import androidx.compose.ui.graphics.Color

// Polina's aesthetic: dark, elegant, warm, serif-inspired
// Inspired by polinalinen.kdnfx.space — dark backgrounds, muted warm tones

// Background
val BackgroundDark = Color(0xFF1A1412)       // Deep warm dark brown
val BackgroundCard = Color(0xFF2A2220)        // Slightly lighter warm dark
val BackgroundCardHover = Color(0xFF332B28)   // Card hover/active

// Text
val TextPrimary = Color(0xFFF5EDE8)           // Warm off-white
val TextSecondary = Color(0xFFB8A99E)         // Muted warm gray
val TextAccent = Color(0xFFE8C9A8)            // Golden warm accent

// Accents — warm bakery tones
val AccentGold = Color(0xFFD4A574)            // Warm gold (primary accent)
val AccentRose = Color(0xFFC4756E)            // Muted rose
val AccentCream = Color(0xFFF0DFC8)           // Cream
val AccentBrown = Color(0xFF8B6F4E)           // Warm brown

// Status
val StatusAction = Color(0xFFD4A574)          // Gold for "action" steps
val StatusWait = Color(0xFF7BA3A8)            // Cool teal for "wait" steps
val StatusActive = Color(0xFFE8C9A8)          // Bright active indicator
val StatusCompleted = Color(0xFF8BAF7C)       // Soft green for done

// Timers
val TimerBackground = Color(0xFF1F1916)       // Timer dark bg
val TimerProgress = Color(0xFFD4A574)         // Gold progress
val TimerUrgent = Color(0xFFC4756E)           // Rose when < 5 min

// Dividers
val DividerColor = Color(0xFF3A302C)          // Subtle warm divider
