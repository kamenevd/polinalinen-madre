package com.polinalinen.madre.ui.theme

import androidx.compose.ui.graphics.Color

// ═══ Polina's aesthetic: warm sourdough bakery tones ═══
// Inspired by fresh bread, mustard seeds, honey, linen

// ═══ DARK THEME COLORS ═══
val DarkBg = Color(0xFF141210)                   // Deep dark — main background
val DarkCard = Color(0xFF1E1A16)                 // Slightly lighter — card bg
val DarkCardHover = Color(0xFF2A2420)            // Card hover/pressed

// ═══ Legacy aliases (used in current code) ═══
val BackgroundDark = DarkBg
val BackgroundCard = DarkCard
val BackgroundCardHover = DarkCardHover

// ═══ Text ═══
val TextPrimary = Color(0xFFF5EDE4)              // Warm white — main text
val TextSecondary = Color(0xFFB5A597)            // Muted warm — secondary
val TextAccent = Color(0xFFE8C9A0)               // Gold — accent text

// ═══ Accents — bakery tones ═══
val AccentGold = Color(0xFFC49A5C)               // Gold
val AccentRose = Color(0xFFC4756E)               // Terracotta
val AccentCream = Color(0xFFF5E6C8)              // Cream
val AccentBrown = Color(0xFF8B6F4E)              // Brown

// ═══ Status ═══
val StatusAction = Color(0xFFC49A5C)             // Gold for "action"
val StatusWait = Color(0xFF6B9DA3)               // Teal for "wait"
val StatusActive = Color(0xFFE8C9A0)             // Gold active
val StatusCompleted = Color(0xFF7FA870)          // Green done

// ═══ Timers ═══
val TimerBackground = Color(0xFF2A2420)          // Timer bg
val TimerProgress = Color(0xFFC49A5C)            // Gold progress
val TimerUrgent = Color(0xFFC4756E)              // Urgent

// ═══ Dividers ═══
val DividerColor = Color(0xFF3A302A)             // Subtle divider

// ═══ Difficulty ═══
val DifficultyEasy = Color(0xFF7FA870)
val DifficultyMedium = Color(0xFFC49A5C)
val DifficultyHard = Color(0xFFC4756E)

// ═══ LIGHT THEME COLORS ═══
val LightBg = Color(0xFFFFF8F0)                  // Warm white
val LightCard = Color(0xFFFFF3E0)                // Light cream
val LightCardHover = Color(0xFFF2E4CC)           // Warm bread

val LightAccentGold = Color(0xFFB8862D)          // Deep gold
val LightAccentRose = Color(0xFFB85C4A)          // Terracotta
val LightAccentCream = Color(0xFFF5E6C8)         // Cream
val LightAccentBrown = Color(0xFF6B4F30)         // Brown

// ═══ Extra ═══
val LinenColor = Color(0xFFE8E0D4)
val CrustDark = Color(0xFF6B4F30)
val CrumbPale = Color(0xFFF5E6C8)
val PolinaNavy = Color(0xFF1B365D)
