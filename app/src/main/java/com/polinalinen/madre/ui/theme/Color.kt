package com.polinalinen.madre.ui.theme

import androidx.compose.ui.graphics.Color

// Polina's aesthetic: dark, elegant, warm, serif-inspired
// Inspired by polinalinen.kdnfx.space + real sourdough photos from family Immich
// Colors extracted from: website palette, bread crusts, linen textures, kitchen

// ═══ Background ═══
val BackgroundDark = Color(0xFF18120E)       // Deep warm brown — like a bakery at night
val BackgroundCard = Color(0xFF261F1A)        // Warm card — like dark wood
val BackgroundCardHover = Color(0xFF302822)   // Card hover — slightly warmer

// ═══ Text ═══
val TextPrimary = Color(0xFFF5EDE4)           // Warm off-white — like flour dust
val TextSecondary = Color(0xFFB5A597)         // Muted warm — like dried dough
val TextAccent = Color(0xFFE8C9A0)            // Golden — like honey on bread

// ═══ Accents — bakery tones from real bread ═══
val AccentGold = Color(0xFFC49A5C)            // Sourdough crust gold (primary accent)
val AccentRose = Color(0xFFC4756E)            // Muted rose — Polina's site accent
val AccentCream = Color(0xFFF0DFC8)           // Cream — bread crumb color
val AccentBrown = Color(0xFF8B6F4E)           // Warm brown — baked bread edge

// ═══ Status ═══
val StatusAction = Color(0xFFC49A5C)          // Gold for "action" steps
val StatusWait = Color(0xFF6B9DA3)            // Soft teal for "wait" steps
val StatusActive = Color(0xFFE8C9A0)          // Bright active indicator
val StatusCompleted = Color(0xFF7FA870)       // Natural green for done

// ═══ Timers ═══
val TimerBackground = Color(0xFF1A1410)       // Timer dark bg
val TimerProgress = Color(0xFFC49A5C)         // Gold progress
val TimerUrgent = Color(0xFFC4756E)           // Rose when < 5 min

// ═══ Dividers ═══
val DividerColor = Color(0xFF3A302A)          // Subtle warm divider

// ═══ Extra — from Polina's palette & bread ═══
val LinenColor = Color(0xFFE8E0D4)            // Linen cloth from baking photos
val CrustDark = Color(0xFF8B6418)             // Dark crust patches
val CrumbPale = Color(0xFFF5E6C8)             // Pale bread crumb
val PolinaNavy = Color(0xFF1B365D)            // Deep navy from polinalinen site
