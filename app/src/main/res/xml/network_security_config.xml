<?xml version="1.0" encoding="utf-8"?>
<!-- Cycle 11: сервер переехал на madre-api.kdnfx.space и отвечает только по
     HTTPS, поэтому исключений для отдельных хостов здесь больше нет —
     открытый текст запрещён целиком, включая отладочные сборки. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
