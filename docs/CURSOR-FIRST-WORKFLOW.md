# Cursor-first workflow — утверждено 20.08.2026

Дима утвердил после голосования 11 моделей (11/11). Этот документ — SSOT ролей. Архитектура приложения остаётся в `CLAUDE.md`.

## Порядок цикла

1. **Продукт:** Дима задаёт смысл и границы; Гес фиксирует проверяемые инварианты.
2. **План и визуал:** Cursor `claude-opus-5-thinking-high`. Фолбэк: Claude Code Opus 5. `auto` запрещён.
3. **Гейт плана, все трое только читают:** GLM-5.3 — данные/приватность; DeepSeek V4 Pro — границы/риски; GPT-5.6 Sol — Android/корректность. Код не начинается до закрытия замечаний или явного решения Димы.
4. **Продакшен-код:** Cursor `gpt-5.3-codex-xhigh`. Фолбэк: Codex CLI `gpt-5.3-codex-spark`. Один автор; тест → код → зелёный тест → тематический коммит.
5. **Гейт кода:** Grok 4.6 — адверсариал; Cursor `gemini-3.1-pro` — визуал. Автор не судит свой артефакт; Gemini заменяется, если участвовал в авторстве.
6. **Лечение замечаний:** тот же Cursor Codex; фолбэк Spark. Гес и судьи Kotlin не правят.
7. **QA/сборка:** Gradle только на 108; Maestro/AVD на 103; визуал смотрит Gemini. Гес запускает и проверяет.
8. **GitHub/подпись:** Гес; версия только через `prepare-version`; required checks; один и тот же подписанный APK/AAB.
9. **Перед магазином:** GPT-5.6 Sol + Grok проверяют подписанный APK/evidence; Дима решает; Гес проверяет live-очередь RuStore.
10. **RuStore/доктор:** Гес; один pending. После выхода Grok + GPT-5.6 + GLM + DeepSeek + Claude; их находки чинит автор следующего цикла.

## Защита от отказов

- В начале цикла проверить live-каталог именованных Cursor-моделей.
- Коротко прогреть фолбэки Claude Code и Codex Spark.
- Записать в evidence: клиент, точный model id/version, роль.
- План хранится файлом, не только внутри чата Cursor.
- Автор и судьи работают в разных сессиях с неизменяемым packet.
- Cursor недоступен → сразу объявленный фолбэк. Если обязательный Claude упёрся в живой 5-часовой лимит — ждём.
